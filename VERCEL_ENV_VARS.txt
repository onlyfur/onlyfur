# IMPORTANT: This file contains the correct configuration for your deployment
# Copy these values to your Vercel environment variables

# Since onlyfur.net and creatorplattform.vercel.app are the same project,
# use relative API paths to avoid CORS issues

# Required for CORS to work with onlyfur.net
FRONTEND_URL=https://onlyfur.net
CORS_ORIGIN=https://onlyfur.net

# Use relative API path since frontend and backend are same domain
VITE_API_BASE_URL=/api

# Database (use your actual production database URL)
DATABASE_URL=your-production-database-url

# JWT Secrets (generate secure random strings)
JWT_SECRET=your-production-jwt-secret-at-least-32-chars
JWT_REFRESH_SECRET=your-production-jwt-refresh-secret-at-least-32-chars

# Google OAuth (use your production Google OAuth credentials)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=https://onlyfur.net/auth/callback/google
VITE_GOOGLE_CLIENT_ID=your-google-client-id

# Server Configuration
NODE_ENV=production
PORT=3000

# Admin Account
ADMIN_EMAIL=admin@onlyfur.net
ADMIN_PASSWORD=your-secure-admin-password
ADMIN_USERNAME=admin
